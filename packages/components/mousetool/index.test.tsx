// import { nextTick, ref } from "vue-demi";
// import { mount } from "@vue/test-utils";
import { TdtMap, TdtMousetool } from "vue-tianditu";

describe("TdtMousetool", () => {
  it("mount", async () => {
    // const mousetoolRef = ref();
    // const wrapper = mount(() => (
    //   <TdtMap center={[113.280637, 23.125178]} zoom={11}>
    //     <TdtMousetool ref={mousetoolRef}></TdtMousetool>
    //   </TdtMap>
    // ));
    // await nextTick();
    // await nextTick();
    // expect(Object.keys(mousetoolRef.value)).toEqual(["open", "close", "clear", "clearAll"]);
  });
});
